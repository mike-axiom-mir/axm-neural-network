[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axm-neural-network"
version = "0.1.0"
description = "Deterministic local-first coordination for AXM neural nodes"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
